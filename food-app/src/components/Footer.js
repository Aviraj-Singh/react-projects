import {LOGO_URL} from "../utils/constants";
import { Link } from "react-router-dom";

const Footer = () => {
    return (
      <div className="flex justify-center w-[100%] mx-auto shadow-lg bg-black text-white mt-10">
        With ❤️ Aviraj
      </div>
    );
  };

  export default Footer;